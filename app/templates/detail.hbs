<ServiceDetail
  @token={{@model.token}}
  @alias={{@model.alias}}
  @url={{@model.url}}
  @icon={{@model.favicon_url}}
  @uptime={{@model.uptime}}
  @down={{@model.down}}
  @downSince={{@model.down_since}}
/>
